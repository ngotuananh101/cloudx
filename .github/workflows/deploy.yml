name: deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  # ─── 1. Run Tests ────────────────────────────────────────────────────────
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.4'
          tools: composer:v2

      - name: Setup Node & pnpm
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'

      - name: Install pnpm
        run: npm install -g pnpm@11.12.0 --ignore-scripts

      - name: Install Dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader
          pnpm install --frozen-lockfile --ignore-scripts

      - name: Copy env & Generate key
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Build assets
        run: pnpm run build

      - name: Run tests
        run: ./vendor/bin/pest

  # ─── 2. Run Lint ────────────────────────────────────────────────────────
  lint:
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.head_ref || github.ref }}

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.4'

      - name: Setup Node & pnpm
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'

      - name: Install pnpm
        run: npm install -g pnpm@11.12.0 --ignore-scripts

      - name: Install Dependencies
        run: |
          composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
          pnpm install --frozen-lockfile --ignore-scripts

      - name: Run Pint
        run: composer lint

      - name: Format Frontend
        run: pnpm run format

      - name: Lint Frontend
        run: pnpm run lint

      - name: Commit Changes
        uses: stefanzweifel/git-auto-commit-action@4a55954c782fc1ea30b9056cd3e7a2b40ca8887d # v7.2.0
        with:
          commit_message: 'chore: fix code style'
          commit_options: '--no-verify'

  # ─── 3. Build and Deploy ────────────────────────────────────────────────
  deploy:
    runs-on: ubuntu-latest
    needs: lint
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          # Pull the latest commit in case the linter pushed changes
          ref: ${{ github.ref }}

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.4'
          tools: composer:v2

      - name: Setup Node & pnpm
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'

      - name: Install pnpm
        run: npm install -g pnpm@11.12.0 --ignore-scripts

      - name: Install production dependencies
        run: |
          composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader
          pnpm install --frozen-lockfile --ignore-scripts

      - name: Build frontend assets
        env:
          VITE_APP_NAME: ${{ vars.VITE_APP_NAME }}
          VITE_PUSHER_HOST: ${{ vars.VITE_PUSHER_HOST }}
          VITE_PUSHER_PORT: ${{ vars.VITE_PUSHER_PORT }}
          VITE_PUSHER_SCHEME: ${{ vars.VITE_PUSHER_SCHEME }}
          VITE_PUSHER_APP_KEY: ${{ vars.VITE_PUSHER_APP_KEY }}
          VITE_PUSHER_APP_CLUSTER: ${{ vars.VITE_PUSHER_APP_CLUSTER }}
        run: pnpm run build

      - name: Sync files to server via rsync
        uses: easingthemes/ssh-deploy@2cc5b27bf3029d0455dd5e09fe02633904031447 # v6.0.3
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_KEY }}
          ARGS: '-avzr --delete --no-o --no-g'
          SOURCE: './'
          REMOTE_HOST: ${{ secrets.SERVER_HOST }}
          REMOTE_USER: ${{ secrets.SERVER_USER }}
          REMOTE_PORT: ${{ secrets.SERVER_PORT || 22 }}
          TARGET: ${{ secrets.DEPLOY_PATH }}
          # Exclude files/folders that shouldn't be overridden or uploaded
          EXCLUDE: '/.git/, /.github/, /node_modules/, /tests/, /.env, /storage/, /deploy.sh, /.user.ini, /.htaccess, /public/.user.ini, /docs/'

      - name: Run post-deploy commands
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}

            # Ensure required storage and bootstrap cache directories exist
            mkdir -p storage/framework/{sessions,views,cache}
            mkdir -p storage/logs
            mkdir -p bootstrap/cache
            chmod -R 775 storage bootstrap/cache || true

            # Enable maintenance mode
            php artisan down --retry=30 --refresh=15 || true

            # Run migrations
            php artisan migrate --force

            # Optimize caches
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            php artisan event:cache

            # Restart queue & storage link
            php artisan storage:link || true
            php artisan queue:restart || true


            # Fix ownership and permissions
            chown -R www:www . || true
            chmod -R 775 storage bootstrap/cache || true

            # Disable maintenance mode
            php artisan up || true

      - name: Notify success
        if: success()
        run: echo "✅ Deployed to production successfully"

      - name: Notify failure
        if: failure()
        run: echo "❌ Deploy failed — check the logs above"
